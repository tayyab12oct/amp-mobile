import { Grid } from '@material-ui/core';
import React from 'react';
import { Theme } from '@material-ui/core/styles';
import { makeStyles } from '@material-ui/styles';

export function SynopsisModal(props) {
  return (
    <div>
      <h1>hello</h1>
      {/* <Grid xs={12} item container className={classes.synopHeader}>
                <Grid xs={11} item>
                  Synopsis : {props.synopTitle}
                </Grid>
                <Grid xs={1} item onClick={() => setModalIsOpen(false)}>
                  <ImageComponent src={Close} alt="" />
                </Grid>
              </Grid>

              <Grid xs={12} container className={classes.advert}>
                <ImageComponent src={ads} alt="" />
              </Grid>
              <div className={classes.synopContent}>
                <Grid xs={12} container>
                  <div className={classes.synopTitle}>
                    {' '}
                    The story takes place in Gotham City, 1981.
                  </div>
                  <div
                    style={{
                      fontSize: '16px',
                      fontFamily: 'Montserrat',
                      height: '100%'
                    }}
                  >
                    <p>
                   {props.synopsis}
                    </p>
                  </div>
                </Grid> */}
    </div>
  );
}
